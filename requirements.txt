langgraph >= 0.6.6
langchain >= 0.3.27
langchain-google-genai >= 2.1.9
python-dotenv >= 1.1.1
IPython >=  9.4.0
lingua-language-detector >= 2.1.1
neo4j >= 5.28.2
networkx >= 3.5
corextopic >= 1.1
scipy >= 1.16.2
keybert >= 0.9.0
flashtext >= 2.7
requests >= 2.25.0
# Configuration and dependency injection
pydantic >= 2.0.0
pydantic_settings >2.10.1
dependency-injector >= 4.41.0
joblib >= 1.5.2
scikit-learn >= 1.7.2
matplotlib >= 3.10.6
nltk >= 3.9.1
bertopic >= 0.17.3
# Enhanced NLP and visualization dependencies
spacy >= 3.8.0
textblob >= 0.18.0
wordcloud >= 1.9.0
seaborn >= 0.13.0
yake >= 0.4.8

# Core testing frameworks
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0

# Coverage reporting
coverage>=7.0.0
pytest-cov>=4.0.0

# Test utilities
responses>=0.23.0  # For mocking HTTP requests if needed
factory-boy>=3.2.0  # For test data factories if needed

# Linting and formatting (for CI/CD)
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Documentation testing
doctest